# Homebridge StartOS package

This repository packages Homebridge for StartOS 0.4.x. Keep `README.md`, `instructions.md`, and `UPDATING.md` in sync with package behavior. Use `TODO.md` for deferred package work. Preserve the `/homebridge` persistent mount and backup coverage when changing the runtime.
