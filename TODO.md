# TODO

- Replace the placeholder `packageRepo` URL in `startos/manifest/index.ts` after creating the remote GitHub repository.
- Pin a tested Homebridge image tag before a public registry release; `latest` is intentionally retained for the requested development package.
- Test HomeKit pairing and mDNS-dependent plugins on physical x86_64 and ARM64 StartOS devices.
